<!doctype html>
<html>
  <head>
    <title>Welcome to <%=shopName%></title>
    <link rel="stylesheet"  type="text/css" href="main.css" />
  </head>
  <body>
    <h1><%=shopName%> Shop</h1>
    <h2>Here are the books that we sell:</h2>
    <% if(orderBy === "DESC") { %>
<a href = "/list?orderBy=ASC">Ascending Order</a> 
<% } else { %>
<a href = "/list?orderBy=DESC">Descending Order</a> 
<% } %>
    <ul>
      <% availableBooks.forEach(function(book) { %>
        <li><%= book.name %>, £<%=book.price%>,<%=book.author%> </li>
      <% }) %>
    </ul>
    </body>
</html>
